name: AutoCheckBJMF

on:
  workflow_dispatch:
  schedule:
    # 每天北京时间 21:10 和 21:20（对应 UTC 13:10 和 13:20）
    - cron: '10,20 13 * * *'

env:
  ClassID: ${{ secrets.ClassID }}
  X: ${{ secrets.X }}
  Y: ${{ secrets.Y }}
  ACC: ${{ secrets.ACC }}
  SearchTime: ${{ secrets.SEARCHTIME }}
  MyCookie: ${{ secrets.MYCOOKIE }}
  token: ${{ secrets.TOKEN }}

jobs:
  checkin:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: python -m pip install --upgrade requests beautifulsoup4 schedule

      - name: Checkin
        run: python main.py
